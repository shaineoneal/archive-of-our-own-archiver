/**
 * Defines the reading status values stored for a work entry.
 * @category Models
 */
export enum WorkStatus {
    Reading = 'reading',
    ToRead = 'toRead',
    Skipped = 'skipped',
    Dropped = 'dropped',
    Read = 'read',
    Default = '' // default
}